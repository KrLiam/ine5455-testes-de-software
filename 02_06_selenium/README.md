
# Como Executar

Inicialize um .venv e instale as dependências utilizando Python 3.10 ou superior.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Execute os testes.

```bash
python3 test_duckduckgo_calculator.py
```
